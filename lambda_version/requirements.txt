# Requirements for AWS Lambda deployment
# Optimized for Lambda runtime environment

# Core trading libraries
ccxt==4.1.64
pandas==2.1.4
numpy==1.24.4

# Technical analysis
ta==0.10.2

# AWS SDK - Note: boto3 is already available in AWS Lambda runtime
# Only specify if you need a specific version
# boto3==1.34.34
# botocore==1.34.34

# For local development, uncomment above lines or install separately:
# pip install boto3

# JSON and data handling
simplejson==3.19.2

# Logging and monitoring
python-json-logger==2.0.7

# HTTP requests (for API calls)
requests==2.31.0
urllib3==2.1.0

# Date/time utilities
python-dateutil==2.8.2
pytz==2023.3

# Decimal handling for DynamoDB
decimal==1.70

# Performance optimization
cython==3.0.6

# Optional: Lightweight ML (if needed)
scikit-learn==1.3.2

# Development/testing tools (exclude in production)
# pytest==7.4.3
# pytest-cov==4.1.0

# Lambda specific optimizations
# Remove heavy dependencies that aren't needed:
# - tensorflow (too large for Lambda)
# - vectorbt (not essential)
# - matplotlib (not needed for serverless)
# - jupyter (not needed)

# Size optimization notes:
# - Use --no-deps flag for specific packages if needed
# - Consider using Lambda Layers for heavy dependencies
# - Total package size should be < 250MB unzipped
